
interface Props {
    ticketData: {
        img: string;
        team: string;
        location: string;
        date: string;
        day: string;
        time: string;
    };
    theme: string;
};

const Ticket = ({ticketData, theme} : Props) => {
    return (
        <div>
        
        </div>
    )
};

export default Ticket;
